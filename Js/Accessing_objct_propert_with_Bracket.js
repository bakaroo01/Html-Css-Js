// Accesing Object properties with Bracket notations

const testObj = {
    "an entree": "hamburger",
    "my side": "veggies",
    "the drink":"water",
};

const entreeValue = testObj["an entree"]
const drinkValue = testObj["the drink"]

// you can't access with the dot notation since ther is aspace in the key properte

// so that's why you use Bracket notation to access them