// Bracket notation last character

// In JavaScript, you can access the last character of a string using bracket notation

// and the index of the last character, which is the length of the string minus one.

// Example of accessing the last character of a string

const myString = "Hello, World!";
const lastCharacter = myString[myString.length - 1];
console.log(lastCharacter); // Output: "!"


